services:
  kaos-dashboard:
    image: ghcr.io/gabrielaleks/kaos-dashboard:latest
    container_name: kaos-dashboard
    restart: unless-stopped
    networks:
      - traefik
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro

networks:
  traefik:
    external: true
